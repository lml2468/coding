/**
 * Template hash tracking types for the Coding update command.
 */

/**
 * Template hashes storage structure
 * Maps relative file paths to their SHA256 hashes
 */
export type TemplateHashes = Record<string, string>;
